[package]
name = "fbi-term-core"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
alacritty_terminal = "0.26"
vte = "0.15"

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
